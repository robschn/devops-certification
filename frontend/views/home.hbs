<div class="container">
    <!-- Example row of columns -->
    <div class="row">
  {{#events}}
        <div class="col-md-4">
            <h2>{{ title }}</h2>
            <p>{{ description }}</p>
        </div>
  {{/events}}
    </div>
</div>